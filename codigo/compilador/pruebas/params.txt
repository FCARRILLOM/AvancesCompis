programa params;

funcion int uno(int : a);
{
	escribe("b: ", a);
	regresa(a);
}

funcion int counter(int : a, int: b);
{
    escribe(a, b, "-----");
	si (a >= b) entonces {
		regresa(a);
	} sino {
        regresa(counter(uno(a) + uno(a), b - 1));
	}
}

principal() {
    escribe(counter(2, 20));
}