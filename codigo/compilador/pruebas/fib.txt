programa fibonacci;
variables
    int : x, i;

principal() {
	escribe("Cantidad de elementos de la seria fibonacci:" );
	lee(x);

	int : f[x];
	f[0] = 0;
	f[1] = 1;

	desde i = 2 hasta i <= x hacer
	{
		f[i] = f[i - 1] + f[i - 2];
	}

	desde i = 0 hasta i < x hacer
	{
		escribe(f[i], "  ");
	}

}